import ProjectsComponent from "../components/Overlay/Projects";

const Projects = ({ theme }) => {
    return (
        <div className="pt-20">
            <ProjectsComponent theme={theme} />
        </div>
    );
};

export default Projects;
